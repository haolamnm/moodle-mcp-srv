{
  "mcpServers": {
    "moodle": {
      "command": "sh",
      "args": [
        "-lc",
        ": \"${MOODLE_API_URL:?Set MOODLE_API_URL}\"; : \"${MOODLE_API_TOKEN:?Set MOODLE_API_TOKEN}\"; exec uvx --from moodle-mcp-srv moodle-mcp"
      ]
    }
  }
}
