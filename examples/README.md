# MCP Client Examples

These examples use placeholder Moodle credentials. Replace `moodle.example.edu` and `replace-me` locally.

Prefer OS environment variables or the client secret store when supported. Do not commit real Moodle tokens or private site URLs.

## Clients

- `claude-desktop.json`
- `vscode.mcp.json`
- `codex.config.toml`
- `opencode.json`
- `droid.md`
